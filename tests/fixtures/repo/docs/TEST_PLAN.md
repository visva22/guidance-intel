# Test Plan

This is documentation, not AI guidance. It should be excluded from AI context.

## Scenario A1

Steps to validate the friends package end to end. Lots of prose here so the
token estimate is non-trivial and the exclusion violation has measurable waste.
Repeated filler content to simulate a real document with meaningful size so that
token-waste math in the report is exercised properly during tests and demos.
